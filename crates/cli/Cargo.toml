[package]
name = "cueward-cli"
version.workspace = true
edition.workspace = true

[[bin]]
name = "cueward"
path = "src/main.rs"

[dependencies]
cueward-core = { path = "../core" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
